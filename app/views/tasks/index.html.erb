<h1>タスク一覧</h1>
<%= link_to 'タスクを登録する', new_task_path%>
<table>
  <tr>
    <th>タスク名</th>
    <th>概要</th>
  </tr>
  <% @tasks.each do | task | %>
  <tr class = "task_row">
    <td><%= task.title %></td>
    <td><%= task.summary %></td>
    <td><%= link_to '詳細', task_path(task.id) %></td>
    <td><%= link_to '編集', edit_task_path(task.id) %></td>
    <td><%= link_to '削除', task_path(task.id), method: :delete ,data: { confirm: '本当に削除してよろしですか？'} %></td>
  </tr>
  <% end %>
</table>